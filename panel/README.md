# panel/ — CEP extension (Phase 2)

Not yet scaffolded. Will contain the CEP panel: `CSXS/manifest.xml`,
panel HTML/JS, `jsx/` ExtendScript for layer generation and animators,
and `animations/` (JSON definitions + pre-rendered preview loops).

See `docs/ARCHITECTURE.md` §1, §3, §4.
